return {
  {
    "ibhagwan/fzf-lua",
    -- dependencies = { "nvim-tree/nvim-web-devicons" }, -- optional
    opts = {
      hls = {
        backdrop = "Normal",
      },
      winopts = {
        backdrop = 100, -- 0=opaque, 100=fully transparent
        border = "rounded", -- Use a visible border
      },
    },
  },
}
